<?xml-stylesheet type="text/xsl" href="format-build-file.xsl"?>
<project name="jpetstore" default="dist" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>

  <target name="test1">
    <!-- Create the time stamp -->
    <echo message ="this test1 works"/>
  </target>
  <target name="test2">
    <!-- Create the time stamp -->
    <echo message ="this test2 works"/>
  </target>
  <target name="test3">
    <!-- Create the time stamp -->
    <echo message ="this test3 works"/>
  </target>
</project>